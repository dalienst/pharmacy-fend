const publicLinks = {
  Home: '/',
  Login: '/login',
  Registration: '/register'
};

const privateLinks = {
  Dashboard: '/dashboard'
}

const urls = {
    REGISTER: "register/",
    LOGIN: "login/"
};

export {urls, publicLinks, privateLinks}; 