const publicLinks = {
  Home: '/',
  Login: '/login',
  Registration: '/register',
  Dashboard: '/dashboard'
};

const privateLinks = {
  Dashboard: '/dashboard'
};

const urls = {
    REGISTER: "register/",
    LOGIN: "login/",
    REFRESH: "login/refresh/"
};

export {urls, publicLinks, privateLinks}; 